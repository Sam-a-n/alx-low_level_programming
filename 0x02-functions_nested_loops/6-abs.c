#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @t:An integer to be computed
 * Return: Absolute value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
